import { CircularProgress } from '@mui/material';

const LoadingCircular = (
  <CircularProgress
    sx={{ position: 'fixed', inset: 0, margin: 'auto' }}
    size={150}
  />
);

export default LoadingCircular;
